/**
 * @zinid/sdk-web — vendor-facing SDK for ZinID hosted verification flows.
 *
 * Phase 0: scaffold only. Public API (emitter, flow instances, channel) lands
 * in later phases. This placeholder exists so all four build outputs generate.
 */
export const SDK_NAME = '@zinid/sdk-web';
