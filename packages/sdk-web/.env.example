# A live frontend session URL, including host and an active session token.
# Used only by e2e/live-session.spec.ts, which skips when this is unset.
# The staging host is temporary — never hardcode it in source.
VITE_SESSION_URL=
